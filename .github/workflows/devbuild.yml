name: 🎹 Development

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:

  build_18:

    name: 🪴 Build 1.18

    runs-on: ubuntu-latest

    steps:
      - name: 🔁 Checkout
        uses: actions/checkout@v2

      - name: 📦 ZIP
        uses: papeloto/action-zip@v1
        with:
          files: assets/ pack.mcmeta pack.png credits.md
          dest: pack.zip
      
      - name: 🏹 Upload
        uses: actions/upload-artifact@v2.3.1
        with:
          name: JqshuvPack-1.18.X
          path: |
            assets/
            pack.mcmeta 
            pack.png 
            credits.md

  build_17:
  
    name: 🪴 Build 1.17

    runs-on: ubuntu-latest

    steps:
      - name: 🔁 Checkout
        uses: actions/checkout@v2
        with:
          ref: 'ver/1.17'

      - name: 📦 ZIP
        uses: papeloto/action-zip@v1
        with:
          files: assets/ pack.mcmeta pack.png credits.md
          dest: pack.zip
      
      - name: 🏹 Upload
        uses: actions/upload-artifact@v2.3.1
        with:
          name: JqshuvPack-1.17.X
          path: |
            assets/
            pack.mcmeta 
            pack.png 
            credits.md
